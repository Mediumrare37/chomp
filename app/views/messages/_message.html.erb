
<div id="message-<%= message.id %>">
  <small>
    <strong><%= message.user.email.split("@")[0].capitalize %></strong><br>
    <i><%= message.created_at.strftime("%a %b %e at %l:%M %p") %></i>
  </small>
  <p><%= message.content %></p>
</div>
